# GitHub Readiness Agent Prompt

You are preparing this repository for GitHub.

Read first:

- `.agents/skills/github-readiness-maintenance/SKILL.md`
- `README.md`
- `AGENTS.md`
- `.agents/registry.yaml`

Check:

- no Markdown links point to local absolute paths
- all internal Markdown links resolve
- `.agents/registry.yaml` points to existing files
- `.gitignore` excludes local machine metadata
- README is usable as the first entrypoint

End with:

- issues fixed
- remaining issues
- validation commands run
- git status summary

