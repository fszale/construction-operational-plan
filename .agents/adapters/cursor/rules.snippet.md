# Cursor Rules Snippet

Use `.agents/registry.yaml` to discover project-local skills and prompts.

Keep code, docs, and plan edits scoped to the assigned task. Do not rewrite canonical plan sections without direction.

