# Codex Adapter Snippet

Read root `AGENTS.md`, then use `.agents/README.md`, `.agents/INDEX.md`, and `.agents/registry.yaml`.

When a task matches a project-local skill, read `.agents/skills/<skill-name>/SKILL.md` before editing.

